@model ProjetoDeBloco.Aplicacao.ViewModels.CursoVM

@{
    ViewBag.Title = "Remover Curso";
}

<ol class="breadcrumb">
    <li>@Html.ActionLink("Home", "Index", "Home")</li>
    <li>@Html.ActionLink("Curso", "Index")</li>
    <li class="active">@ViewBag.Title</li>
</ol>


<h2>@ViewBag.Title</h2>

<h3>Tem certeza que deseja excluir esse registro?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.CodigoENome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CodigoENome)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Remover" class="btn btn-primary" /> 
            @Html.ActionLink("Voltar a Consulta", "Index", null, new { @class = "btn btn-default" })
        </div>
    }
</div>
